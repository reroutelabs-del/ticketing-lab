from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return 'Ticketing System Web App is Live! ✅'

if __name__ == '__main__':
    print("✅ Flask app is starting...")
    app.run(host='0.0.0.0', port=5000, debug=True)
